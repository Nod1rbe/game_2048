import 'package:flutter/material.dart';

class GameTheme {
  static const gradientStart = Color(0xFF3A67F1);
  static const gradientEnd = Color(0xFFB148F2);

  static const bg = Color(0xFF1A1A2E);
  static const surface = Color(0xFFFFFFFF);
  static const surfaceHigh = Color(0xFFF0F4FF);
  static const border = Color(0xFFE0E7FF);
  static const accent = Color(0xFF3A67F1);
  static const accentDim = Color(0xFF2A4A8B);
  static const gold = Color(0xFFFFD700);
  static const white = Colors.white;
  static const black = Colors.black;
  static const danger = Color(0xFFFF4444);

  static const fontDisplay = 'monospace';
}
